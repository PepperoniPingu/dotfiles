cursor_shape beam
cursor_shape_unfocused beam
cursor_blink_interval 0.5
cursor_stop_blinking_after 0
confirm_os_window_close 0
# X11 backend so mutter draws the GNOME title bar (Wayland = kitty CSD).
# XWayland reports trackpad scroll ~9x smaller than native Wayland; compensate.
linux_display_server x11
touch_scroll_multiplier 9.0
notify_on_cmd_finish never

map ctrl+w close_window
map ctrl+left resize_window narrower
map ctrl+right resize_window wider
map ctrl+up resize_window taller
map ctrl+down resize_window shorter
map ctrl+shift+z toggle_layout stack
allow_remote_control yes

map ctrl+shift+i remote_control set-colors --all --configured ~/.config/kitty/kanagawa.conf
map ctrl+shift+o remote_control set-colors --all --configured ~/.config/kitty/kanagawa-lotus.conf

include kanagawa.conf
